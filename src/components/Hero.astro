---
import { siteConfig } from '@/data/siteConfig';
import Spotlight from './ui/Spotlight.astro';
import { getImagePositionClass } from '@/lib/utils';
import { makeTelHref } from '@/lib/links';

type Props = {
  /** Optional fully-qualified (BASE_URL-prefixed) hero image URL. */
  imageSrc?: string;
  /** Optional srcset string for the hero image (URLs should already include BASE_URL). */
  imageSrcSet?: string | undefined;
  /** Optional sizes attribute when using srcset. */
  imageSizes?: string | undefined;

  /** Optional mobile-only hero image URL (BASE_URL-prefixed). Enables art-direction via <picture>. */
  imageSrcMobile?: string;
  /** Optional mobile-only srcset string (URLs should already include BASE_URL). */
  imageSrcSetMobile?: string | undefined;
  /** Optional mobile-only sizes attribute when using the mobile srcset. */
  imageSizesMobile?: string | undefined;

  /** CSS `object-position` value for the hero image, e.g. "50% 30%". */
  imagePosition?: string;
};

const {
  imageSrc,
  imageSrcSet,
  imageSizes,
  imageSrcMobile,
  imageSrcSetMobile,
  imageSizesMobile,
  imagePosition = '50% 50%',
} = Astro.props;

const base = import.meta.env.BASE_URL;
const telHref = makeTelHref(siteConfig.contact.phoneE164);
// Opt-in only: avoids 404 network requests when the optional hero videos are not present.
const heroVideoOptIn =
  String(import.meta.env.PUBLIC_ENABLE_HERO_VIDEO ?? '').toLowerCase() === 'true';
const heroFallback = imageSrc ?? imageSrcMobile ?? '';
const heroImg = imageSrc ?? imageSrcMobile ?? heroFallback;

const imagePositionClass = getImagePositionClass(imagePosition);

const heroVideoWebm = `${base}media/hero-cleaning.webm`;
const heroVideoMp4 = `${base}media/hero-cleaning.mp4`;
---

<section
  class="hero-splash hero-photo relative w-full overflow-hidden antialiased"
  data-hero-section
>
  {/* Top-down gradient overlay for header readability */}
  <div
    aria-hidden="true"
    class="pointer-events-none absolute inset-x-0 top-0 z-10 h-32 bg-gradient-to-b from-black/40 to-transparent opacity-60"
  >
  </div>
  <div aria-hidden="true" class="pointer-events-none absolute inset-0">
    {/* Always render a lightweight image base (also acts as the video poster / fallback). */}
    {
      imageSrcMobile || imageSrcSetMobile ? (
        <picture>
          <source
            media="(max-width: 639px)"
            srcset={imageSrcSetMobile ?? imageSrcMobile}
            sizes={imageSrcSetMobile ? imageSizesMobile : undefined}
          />
          <img
            src={heroImg}
            alt="Office cleaning team sanitizing commercial workspace in San Francisco"
            srcset={imageSrcSet}
            sizes={imageSrcSet ? imageSizes : undefined}
            class:list={[
              'h-full w-full object-cover opacity-[0.82] sm:opacity-[0.88]',
              imagePositionClass,
            ]}
            loading="eager"
            fetchpriority="high"
            decoding="async"
            onerror={`if (this.src !== '${heroFallback}') this.src = '${heroFallback}'`}
          />
        </picture>
      ) : (
        <img
          src={heroImg}
          alt="Office cleaning team sanitizing commercial workspace in San Francisco"
          srcset={imageSrcSet}
          sizes={imageSrcSet ? imageSizes : undefined}
          class:list={[
            'h-full w-full object-cover opacity-[0.82] sm:opacity-[0.88]',
            imagePositionClass,
          ]}
          loading="eager"
          fetchpriority="high"
          decoding="async"
          onerror={`if (this.src !== '${heroFallback}') this.src = '${heroFallback}'`}
        />
      )
    }

    {/* Optional background video (muted + playsInline + loop). */}
    {
      heroVideoOptIn && (
        <video
          class="absolute inset-0 hidden h-full w-full object-cover opacity-[0.14]"
          muted
          loop
          playsinline
          preload="metadata"
          poster={heroImg}
          disablepictureinpicture
          data-hero-video
        >
          <source src={heroVideoWebm} type="video/webm" />
          <source src={heroVideoMp4} type="video/mp4" />
        </video>
      )
    }

    {/* Mobile-first readability veil with brand-tinted overlay (token-based for consistency). */}
    <div
      class="absolute inset-0 bg-gradient-to-b from-[rgb(var(--color-text))/0.45] via-[rgb(var(--color-text))/0.22] to-[rgb(var(--color-brand))/0.08] dark:from-[rgb(var(--color-text))/0.6] dark:via-[rgb(var(--color-text-subtle))/0.3] sm:from-[rgb(var(--color-text))/0.38] sm:via-[rgb(var(--color-text))/0.18]"
    >
    </div>
  </div>

  {/* Match the inner-page hero corner accent (used on Services) for visual consistency. */}
  <Spotlight class="-top-40 left-0 md:-top-20 md:left-60" fill="white" />

  <div
    class="container-page relative z-10 pb-20 pt-24 sm:pb-24 sm:pt-28 lg:pb-36 lg:pt-44 xl:pb-40 xl:pt-48"
  >
    <div class="mx-auto max-w-4xl text-center lg:max-w-5xl">
      <h1
        class="heading-1 animate-slide-up text-balance font-serif text-white drop-shadow-md lg:text-5xl xl:text-6xl"
        data-hero-headline
      >
        <span>Real people keeping your office steady.</span>
      </h1>
      <p
        class="mx-auto mt-6 max-w-2xl animate-slide-up text-balance text-lg font-medium leading-relaxed text-white/90 drop-shadow-sm [animation-delay:200ms] md:text-xl lg:mt-8 lg:max-w-3xl lg:text-2xl"
        data-hero-subhead
      >
        Local, owner-led cleaning in <span class="font-bold text-white"
          >{siteConfig.brand.city}</span
        >. Same crew, photo updates, and a real person answering when you call.
      </p>
      <div
        class="mt-10 flex animate-slide-up flex-col items-center justify-center gap-4 [animation-delay:400ms] sm:flex-row lg:mt-12 lg:gap-5"
      >
        <a
          href={telHref}
          class="btn-primary btn-premium shadow-accent-900/30 w-full min-w-[180px] shadow-lg sm:w-auto lg:min-w-[200px] lg:px-8 lg:py-4 lg:text-base"
          data-haptic="true"
        >
          Call now
        </a>
        <a
          href={`${base}contact/`}
          class="btn-secondary btn-premium w-full min-w-[180px] sm:w-auto lg:min-w-[200px] lg:px-8 lg:py-4 lg:text-base"
          data-haptic="true"
        >
          Request a quote
        </a>
      </div>
      <div
        class="mt-9 grid animate-slide-up gap-4 text-left [animation-delay:520ms] lg:mt-12 lg:grid-cols-[1.1fr_0.9fr] lg:items-stretch lg:gap-6"
      >
        <div class="owner-note">
          <p class="owner-note__eyebrow">Owner note</p>
          <p class="owner-note__body">
            "We show up with the same faces, walk the space with you, and send a quick text with a
            photo when we're done. If something feels off, you call and I pick up."
          </p>
          <div class="owner-note__meta">
            <span class="owner-note__dot" aria-hidden="true"></span>
            <span>Owner-led crew</span>
            <span class="owner-note__divider" aria-hidden="true">•</span>
            <span>Direct: {siteConfig.contact.phoneDisplay}</span>
          </div>
        </div>
        <div class="visit-promise">
          <p class="visit-promise__title">What stays consistent</p>
          <ul class="visit-promise__list">
            <li>Walkthrough before the first clean</li>
            <li>Photos + notes after each visit</li>
            <li>Supply restock reminders you can reply to</li>
          </ul>
          <div class="visit-promise__footnote">
            No bots, no gimmicks-just a reliable crew you can text.
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .owner-note {
    position: relative;
    border-radius: 1rem;
    padding: 1.25rem 1.25rem 1.5rem;
    background: linear-gradient(
      180deg,
      rgb(var(--color-surface)) 0%,
      rgb(var(--color-accent-50)) 110%
    );
    border: 1px solid rgb(var(--color-border) / 0.7);
    box-shadow: 0 16px 50px -32px rgb(0 0 0 / 0.7);
    color: rgb(var(--color-text));
    overflow: hidden;
  }

  .owner-note::after {
    content: '';
    position: absolute;
    inset: 0;
    background-image: var(--asset-noise);
    opacity: 0.16;
    pointer-events: none;
  }

  .owner-note__eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.8rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: rgb(var(--color-brand-700));
  }

  .owner-note__eyebrow::before {
    content: '';
    width: 1.5rem;
    height: 1px;
    background: rgb(var(--color-brand));
    display: inline-block;
  }

  .owner-note__body {
    margin-top: 0.45rem;
    font-size: 1.02rem;
    line-height: 1.6;
    font-weight: 600;
    color: rgb(var(--color-text));
  }

  .owner-note__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.5rem;
    margin-top: 0.85rem;
    font-size: 0.92rem;
    color: rgb(var(--color-text-muted));
  }

  .owner-note__dot {
    width: 0.65rem;
    height: 0.65rem;
    border-radius: 999px;
    background: linear-gradient(135deg, rgb(var(--color-accent-500)), rgb(var(--color-brand-500)));
    box-shadow: 0 0 0 4px rgb(var(--color-accent-200) / 0.4);
  }

  .owner-note__divider {
    color: rgb(var(--color-text-subtle));
    opacity: 0.6;
  }

  .visit-promise {
    position: relative;
    padding: 1.25rem 1.25rem 1.35rem;
    background:
      linear-gradient(
        120deg,
        rgb(var(--color-accent-100) / 0.7),
        rgb(var(--color-accent-200) / 0.4)
      ),
      linear-gradient(180deg, rgb(var(--color-surface) / 0.96), rgb(var(--color-surface)));
    border-radius: 1rem;
    color: rgb(var(--color-text));
    border: 1px solid rgb(var(--color-border) / 0.7);
  }

  .visit-promise__title {
    font-size: 0.95rem;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    font-weight: 800;
    color: rgb(var(--color-brand-800));
  }

  .visit-promise__list {
    margin-top: 0.65rem;
    display: grid;
    gap: 0.45rem;
    padding-left: 0;
    list-style: none;
  }

  .visit-promise__list li {
    display: grid;
    grid-template-columns: auto 1fr;
    align-items: start;
    gap: 0.55rem;
    font-weight: 600;
    line-height: 1.5;
  }

  .visit-promise__list li::before {
    content: '✓';
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 1.35rem;
    height: 1.35rem;
    border-radius: 999px;
    background: rgb(var(--color-brand-100));
    color: rgb(var(--color-brand-800));
    font-weight: 800;
    box-shadow: 0 6px 14px -10px rgb(0 0 0 / 0.35);
  }

  .visit-promise__footnote {
    margin-top: 0.85rem;
    font-size: 0.92rem;
    color: rgb(var(--color-text-muted));
  }

  @media (max-width: 640px) {
    .owner-note,
    .visit-promise {
      box-shadow: 0 16px 40px -28px rgb(0 0 0 / 0.85);
    }
  }
</style>

<script>
  // Progressive video enhancement
  const initVideo = () => {
    const video = document.querySelector<HTMLVideoElement>('[data-hero-video]');
    if (!video) return;

    const shouldEnable = () => {
      // Respect reduced motion
      const motion = window.matchMedia('(prefers-reduced-motion: reduce)');
      if (motion.matches) return false;

      // Respect data saver
      const nav = navigator as Navigator & {
        connection?: { saveData?: boolean; effectiveType?: string };
      };
      if (nav.connection?.saveData) return false;

      const type = (nav.connection?.effectiveType ?? '').toLowerCase();
      if (type.includes('2g')) return false;

      return true;
    };

    if (shouldEnable()) {
      video.classList.remove('hidden');
      video.play().catch(() => {
        // Autoplay failed or blocked
        video.classList.add('hidden');
      });

      // Track play once
      const onPlay = () => {
        if (window.damraTrack) {
          window.damraTrack('video_play', { video: 'hero' });
        }
        video.removeEventListener('play', onPlay);
      };
      video.addEventListener('play', onPlay);

      // If error, hide it
      video.addEventListener('error', () => {
        video.classList.add('hidden');
      });
    }
  };

  const clamp = (v: number, min: number, max: number) => Math.max(min, Math.min(max, v));

  const initHeroMotion = () => {
    const hero = document.querySelector<HTMLElement>('[data-hero-section]');
    if (!hero) return;

    const isWebDriver = (navigator as Navigator & { webdriver?: boolean }).webdriver === true;
    const reduceMotion =
      (window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches ?? false) || isWebDriver;
    const headline = hero.querySelector<HTMLElement>('[data-hero-headline]');
    const subhead = hero.querySelector<HTMLElement>('[data-hero-subhead]');

    // Parallax text overlay (subtle, mobile-first). Disabled for reduced motion.
    if (!reduceMotion && headline && subhead) {
      let ticking = false;
      const onScroll = () => {
        if (ticking) return;
        ticking = true;
        window.requestAnimationFrame(() => {
          ticking = false;
          const rect = hero.getBoundingClientRect();
          const progress = clamp(-rect.top / Math.max(1, rect.height), 0, 1);
          // Headline moves slightly slower than scroll.
          const hY = progress * -18;
          const pY = progress * -12;
          headline.style.transform = `translate3d(0, ${hY}px, 0)`;
          subhead.style.transform = `translate3d(0, ${pY}px, 0)`;
        });
      };

      window.addEventListener('scroll', onScroll, { passive: true });
      window.addEventListener('resize', onScroll);
      onScroll();
    }
  };

  // Run on load
  initVideo();
  initHeroMotion();
  // Also run on view transitions if enabled later
  document.addEventListener('astro:page-load', initVideo);
  document.addEventListener('astro:page-load', initHeroMotion);
</script>
