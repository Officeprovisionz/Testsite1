---
// NOTE: The "Service levels"/"Scope" section was removed from the site.
// This component remains as a no-op to avoid breaking any stale imports,
// but it intentionally renders nothing.

type Props = {
  id?: string | undefined;
  heading?: string;
  description?: string | undefined;
  ctaHref?: string | undefined;
  ctaLabel?: string;
};

void (Astro.props as Props);
---

<!-- Service levels section removed -->
