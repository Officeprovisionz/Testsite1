---
import { getCollection } from 'astro:content';
import WaveDivider from '@/components/WaveDivider.astro';
import SectionHeader from '@/components/ui/SectionHeader.astro';

type Props = {
  id?: string;
  heading?: string;
  description?: string;
  showWave?: boolean;
};

const {
  id = 'faq',
  heading = 'FAQ',
  description = 'Quick answers to common questions.',
  showWave = true,
} = Astro.props;

const faqs = await getCollection('faqs');
const items = faqs.map((f) => f.data);

const searchId = `${id}-search`;

const faqJsonLd = (() => {
  const data = {
    '@context': 'https://schema.org',
    '@type': 'FAQPage',
    mainEntity: items.map((it) => ({
      '@type': 'Question',
      name: it.question,
      acceptedAnswer: {
        '@type': 'Answer',
        text: it.answer,
      },
    })),
  };

  // Prevent accidental "</script>" injection via safe escaping.
  return JSON.stringify(data).replace(/</g, '\\u003c');
})();
---

<section id={id} class="section scroll-mt-20">
  <script type="application/ld+json" is:inline set:html={faqJsonLd} />
  <div class="container-page relative">
    <SectionHeader eyebrow="FAQ" heading={heading} description={description} />

    <div class="mx-auto mt-10 max-w-3xl lg:mt-14 lg:max-w-4xl" data-faq>
      <div class="mb-4 lg:mb-6">
        <label class="sr-only" for={searchId}>Search FAQs</label>
        <div class="relative">
          <input
            id={searchId}
            type="search"
            inputmode="search"
            autocomplete="off"
            placeholder="Search questions…"
            class="text-strong focus:border-brand/50 bg-surface/90 placeholder:text-muted w-full rounded-(--radius-control) border border-[rgb(var(--color-border)/0.7)] px-4 py-3 pr-10 transition focus:ring-2 focus:ring-[rgb(var(--color-brand)/0.3)] focus:outline-none"
            data-faq-search
          />
          <svg
            aria-hidden="true"
            class="text-muted pointer-events-none absolute top-1/2 right-3 h-5 w-5 -translate-y-1/2"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              d="M21 21l-4.35-4.35m1.35-5.65a7 7 0 11-14 0 7 7 0 0114 0z"></path>
          </svg>
        </div>
        <p class="text-muted mt-2 text-xs" aria-live="polite" data-faq-status></p>
      </div>

      <div class="space-y-3" data-faq-list>
        {
          items.map((it) => (
            <details
              class="group overflow-hidden rounded-(--radius-card) border border-[rgb(var(--color-border)/0.55)] bg-[rgb(var(--color-surface)/0.9)] shadow-(--shadow-xs) transition-all duration-250 open:border-[rgb(var(--color-brand)/0.3)] open:shadow-(--shadow-sm)"
              data-faq-item
            >
              <summary class="text-strong flex w-full cursor-pointer list-none items-center justify-between gap-4 px-5 py-4.5 text-left text-[0.9375rem] font-semibold transition-colors duration-200 hover:text-[rgb(var(--color-brand-700))] focus-visible:ring-2 focus-visible:ring-[rgb(var(--color-brand)/0.45)] focus-visible:ring-offset-2 focus-visible:outline-none lg:px-6 lg:py-5 lg:text-base">
                <span data-faq-q>{it.question}</span>
                <span class="text-brand ml-2 flex h-6 w-6 shrink-0 items-center justify-center rounded-lg bg-[rgb(var(--color-brand)/0.08)] transition-all duration-250 group-open:rotate-180 group-open:bg-[rgb(var(--color-brand)/0.12)]">
                  <svg
                    aria-hidden="true"
                    data-faq-icon
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                    stroke-width="2.5"
                    class="h-3.5 w-3.5"
                  >
                    <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7" />
                  </svg>
                </span>
              </summary>
              <div
                class="text-muted border-t border-[rgb(var(--color-border)/0.45)] bg-[rgb(var(--color-surface-muted)/0.4)] px-5 py-4 text-sm leading-relaxed lg:px-6 lg:py-5 lg:text-[0.9375rem] lg:leading-relaxed"
                data-faq-a
              >
                <p>{it.answer}</p>
              </div>
            </details>
          ))
        }
      </div>

      <div
        class="border-app/40 bg-surface/80 text-muted mt-4 hidden rounded-(--radius-card) border px-5 py-4 text-sm"
        data-faq-empty
      >
        No matches. Try a different keyword (e.g. “pricing”, “supplies”, “after-hours”).
      </div>
    </div>
  </div>

  {
    showWave ? (
      <div class="relative mt-12 -mb-px">
        <WaveDivider tone="surface-muted" class="absolute right-0 bottom-0 left-0" />
      </div>
    ) : null
  }

  <script>
    (() => {
      const init = () => {
        const root = document.querySelector('[data-faq]') as HTMLElement | null;
        if (!root) return;
        if (root.dataset.faqInit === 'true') return;
        root.dataset.faqInit = 'true';

        const input = root.querySelector('[data-faq-search]') as HTMLInputElement | null;
        const status = root.querySelector('[data-faq-status]') as HTMLElement | null;
        const empty = root.querySelector('[data-faq-empty]') as HTMLElement | null;
        const items = Array.from(root.querySelectorAll<HTMLDetailsElement>('[data-faq-item]'));

        const norm = (value: string) => value.trim().toLowerCase();
        const setStatus = (text: string) => {
          if (!status) return;
          status.textContent = text;
        };

        items.forEach((el) => {
          const q = el.querySelector('[data-faq-q]')?.textContent ?? '';
          const a = el.querySelector('[data-faq-a]')?.textContent ?? '';
          el.dataset.searchText = norm(`${q} ${a}`);
        });

        const applyFilter = (queryRaw: string, opts: { openFirst?: boolean } = {}) => {
          const query = norm(queryRaw);
          let visible = 0;

          items.forEach((el) => {
            const hay = el.dataset.searchText ?? '';
            const match = !query || hay.includes(query);
            el.hidden = !match;
            if (!match && el.open) el.open = false;
            if (match) {
              visible += 1;
            }
          });

          // Determine the first visible match after filtering.
          const first = items.find((el) => !el.hidden);

          if (empty) empty.classList.toggle('hidden', visible !== 0);

          if (!query) {
            setStatus('');
            return;
          }

          setStatus(`${visible} result${visible === 1 ? '' : 's'} for “${queryRaw.trim()}”`);

          if (opts.openFirst && first) {
            first.open = true;
            first.scrollIntoView({ block: 'nearest', behavior: 'smooth' });
          }
        };

        items.forEach((el) => {
          el.addEventListener('toggle', () => {
            // Keep it "dropdown" style: only one open at a time.
            if (el.open) {
              items.forEach((other) => other !== el && (other.open = false));

              // Track FAQ interaction
              const question = el.querySelector('[data-faq-q]')?.textContent || 'unknown';
              try {
                window.damraTrack?.('faq_open', { question });
              } catch {
                // ignore
              }
            }
          });
        });

        if (input) {
          input.addEventListener('input', () => applyFilter(input.value));
          input.addEventListener('keydown', (ev) => {
            if ((ev as KeyboardEvent).key === 'Enter') {
              applyFilter(input.value, { openFirst: true });
            }
          });
        }

        // Initial state
        applyFilter(input?.value ?? '');
      };

      const doc = document.documentElement;
      if (doc.dataset.faqListen !== 'true') {
        doc.dataset.faqListen = 'true';
        document.addEventListener('astro:page-load', init);
      }

      init();
    })();
  </script>
</section>

<style>
  /* Remove the default disclosure triangle marker for a cleaner dropdown look. */
  summary::-webkit-details-marker {
    display: none;
  }
</style>
